/* Write your T-SQL query statement below */


-- select name,
-- sum(isnull(rest,0)) rest,
-- sum(isnull(paid,0)) paid,
-- sum(isnull(canceled,0)) canceled,
-- sum(isnull(refunded,0)) refunded
-- from Product p 
-- left join Invoice i on p.product_id = i.product_id
-- group by name
-- order by name




